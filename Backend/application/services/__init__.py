"""Application services package.

This package intentionally avoids eager re-export imports to prevent
import-time side effects and circular dependencies. Consumers should import
service modules explicitly.
"""

